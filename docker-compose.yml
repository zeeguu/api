version: "3.8"

services:
  dev_server:
    image: zeeguu_api_dev
    environment:
      ZEEGUU_CONFIG: /Zeeguu-API/default_docker.cfg
      ZEEGUU_ES_CONN_STRING: "elasticsearch:9200"
      PYTHONUNBUFFERED: 1
      MICROSOFT_TRANSLATE_API_KEY: ${MICROSOFT_TRANSLATE_API_KEY}
      GOOGLE_TRANSLATE_API_KEY: ${GOOGLE_TRANSLATE_API_KEY}

    ports:
      - 9001:9001
    volumes:
      - .:/Zeeguu-API
      - /tmp:/zeeguu-data
    entrypoint: "python /Zeeguu-API/dev.py"
    networks:
      - zeeguu_backend
    depends_on:
      - elasticsearch


  elasticsearch:
    image: elasticsearch:7.6.2
    platform: linux/amd64
    ports:
      - 9200:9200
      - 9300:9300
    environment:
      - discovery.type=single-node
      - network.host=0.0.0.0 # is this still needed? 
    volumes:
      - /Users/gh/zeeguu/api/dev_data_folder/elasticsearch_db/data:/usr/share/elasticsearch/data
    networks:
      - zeeguu_backend
    restart: unless-stopped



  dev_play:
    image: zeeguu_api_dev
    environment:
      ZEEGUU_CONFIG: /Zeeguu-API/default_docker.cfg

    volumes:
      - .:/Zeeguu-API
      - /tmp:/zeeguu-data
    entrypoint: "python tools/_playground.py"
    networks:
      - zeeguu_backend

  # docker-compose run --rm dev_bash
  dev_bash:
    image: zeeguu_api_dev
    stdin_open: true # docker run -i
    tty: true # docker run -t

    environment:
      ZEEGUU_CONFIG: /Zeeguu-API/default_docker.cfg

    volumes:
      - .:/Zeeguu-API
      - /tmp:/zeeguu-data
    entrypoint: "bash"
    networks:
      - zeeguu_backend

  dev_test:
    image: zeeguu_api_dev
    environment:
      ZEEGUU_CONFIG: /Zeeguu-API/default_docker.cfg

    volumes:
      - .:/Zeeguu-API
      - /tmp:/zeeguu-data
    entrypoint: "./run_tests.sh"
    networks:
      - zeeguu_backend

  dev_init_es:
    image: zeeguu_api_dev
    environment:
      ZEEGUU_CONFIG: /Zeeguu-API/default_docker.cfg
      ZEEGUU_ES_CONN_STRING: "elasticsearch:9200"

    volumes:
      - .:/Zeeguu-API
      - /tmp:/zeeguu-data
      - /Users:/userslalal
    entrypoint: "python tools/mysql_to_elastic.py"
    networks:
      - zeeguu_backend
    depends_on:
      - elasticsearch


networks:
  zeeguu_backend:
