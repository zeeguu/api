# Master requirements file - includes both base and app requirements
# For Docker: Split into two layers for better caching
# For local dev: Install all at once with `pip install -r requirements.txt`

-r requirements-base.txt
-r requirements-app.txt